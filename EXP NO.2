#include <stdio.h>
#include <fonti.h>
#include <unistd.h>
int main () {
   int fd1,fd2 n;
   char source [50], dest[50];
   char buffer [1024];
   Printf ("Enter source file name:");
   scanf(" %s", &source);
   printf ("Enter destination file name?);
   Scanf("%s", dest);
fd = open (sarce, O-RDONLY);
if (fd. 20) {
   printf ("Error: can not opon destination file In");
   return 1;
  }
 fd₂= open(dest, O-WRONLY | O-LREAT, 0644);
if (fd2<0) {
    Printf ("error: connet open destination file in");
    return 1;
}
while ((n= read (fd, buffer, size of Cbuffer))) >0) {
 write (fd2, buffer,n);
}
close (fd1;
Close (fd2);
print(" file copied successfully.in");
return 0; 
}
